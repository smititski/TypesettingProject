<Window x:Class="UI_Application.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:UI_Application.ViewModels"
        xmlns:converters="clr-namespace:UI_Application.Converters"
        mc:Ignorable="d"
        Title="מערכת עימוד תורנית מקצועית" 
        Height="900" Width="1400"
        FlowDirection="RightToLeft"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <converters:PageInfoConverter x:Key="PageInfoConverter" />
    </Window.Resources>

    <Window.DataContext>
        <vm:EditorViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0" Height="45">
            <Button Content="בצע עימוד" Command="{Binding LayoutCommand}" Padding="15,8" Margin="5" FontWeight="Bold"/>
            <Separator/>
            <TextBlock Text="תווים: " VerticalAlignment="Center" Margin="5,0"/>
            <TextBlock Text="{Binding Text.Length}" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBlock Text="עמודים: " VerticalAlignment="Center" Margin="5,0"/>
            <TextBlock Text="{Binding Pages.Count}" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
        </ToolBar>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350" MinWidth="300"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300" MinWidth="250"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <!-- Area 1: Input -->
            <Border Grid.Column="0" BorderBrush="#3F51B5" BorderThickness="2" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="אזור 1: קלט טקסט גולמי" FontWeight="Bold" Padding="5" Background="#3F51B5" Foreground="White"/>
                    <TextBlock Grid.Row="1" Text="תגים: $$הערה א'$$ %%הערה ב'%% &amp;&amp;הערה ג'&amp;&amp;" FontSize="11" Foreground="Gray" Margin="5" TextWrapping="Wrap"/>
                    <TextBox Grid.Row="2" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="13" TextAlignment="Right" Padding="8" Margin="5"/>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>

            <!-- Area 2: Processed Output -->
            <Border Grid.Column="2" BorderBrush="#4CAF50" BorderThickness="2" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="2*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="אזור 2: ניתוח טקסט" FontWeight="Bold" Padding="5" Background="#4CAF50" Foreground="White"/>
                    <TextBlock Grid.Row="1" Text="טקסט ראשי:" FontWeight="Bold" Margin="5,2" FontSize="11"/>
                    <TextBox Grid.Row="1" Text="{Binding ExtractedMainText, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Frank Ruehl" FontSize="14" Margin="5,20,5,5" Background="#F5F5F5"/>
                    <TextBlock Grid.Row="2" Text="הערות א' ($$):" FontWeight="Bold" Margin="5,2" FontSize="11" Background="#E3F2FD"/>
                    <ListBox Grid.Row="3" ItemsSource="{Binding ExtractedFootnotesA}" BorderThickness="1" BorderBrush="#E3F2FD" Margin="5,0,5,2"/>
                    <TextBlock Grid.Row="4" Text="הערות ב' (%%):" FontWeight="Bold" Margin="5,2" FontSize="11" Background="#E8F5E9"/>
                    <ListBox Grid.Row="5" ItemsSource="{Binding ExtractedFootnotesB}" BorderThickness="1" BorderBrush="#E8F5E9" Margin="5,0,5,2"/>
                    <TextBlock Grid.Row="6" Text="הערות ג' (&amp;&amp;):" FontWeight="Bold" Margin="5,2" FontSize="11" Background="#FFF9C4"/>
                    <ListBox Grid.Row="7" ItemsSource="{Binding ExtractedFootnotesC}" BorderThickness="1" BorderBrush="#FFF9C4" Margin="5,0,5,5"/>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch"/>

            <!-- Area 3: Preview -->
            <Border Grid.Column="4" BorderBrush="#FF9800" BorderThickness="2" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="אזור 3: תצוגה מקדימה" FontWeight="Bold" Padding="5" Background="#FF9800" Foreground="White"/>
                    <Grid Grid.Row="1" Background="#FFF3E0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="עמוד:" Margin="5" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding Pages}" SelectedItem="{Binding CurrentPage}" DisplayMemberPath="PageNumber" Margin="5" VerticalAlignment="Center"/>
                    </Grid>
                    <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Margin="5">
                        <Canvas x:Name="GlyphCanvas" Background="White" MinWidth="600" MinHeight="700"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <StatusBar Grid.Row="2" Height="30">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" FontSize="12"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
